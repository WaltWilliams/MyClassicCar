<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.woodlandcoders.myclassiccar"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- The android:required="false" was added so this app would be compatible everything.-->
    <uses-feature  android:name="android.hardware.camera" android:required="false"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CarAddActivity" />
        <activity android:name=".CarInfoActivity" />
        <activity android:name=".CarEditActivity" />
        <activity android:name=".ViewPartsActivityRV" />
        <activity android:name=".OemPartAddActivity" />
        <activity android:name=".SinglePartActivity" />
        <activity android:name=".TPMAddPartActivity" />
        <activity android:name=".TPMPartActivity" />
        <activity android:name=".OemPartActivity" />
        <activity android:name=".TPMEditPartActivity" />
        <activity android:name=".OemPartEditActivity" />
        <activity android:name=".BackupActivity" />
        <activity android:name=".ExistingPartActivityRV"/>
        <activity android:name=".HelpActivity" />
        <activity android:name=".RecordSelectActivity" />
        <activity android:name=".MaintenanceEditActivity" />
        <activity android:name=".MaintenanceAddActivity" />
        <activity android:name=".MaintenanceActivity" />
        <activity android:name=".OilChangeEditActivity" />
        <activity android:name=".OilChangeAddActivity" />
        <activity android:name=".OilChangeActivity" />
    </application>

</manifest>